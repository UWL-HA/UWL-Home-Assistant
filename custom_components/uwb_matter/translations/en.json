{
  "config": {
    "step": {"user": {"title": "UltraWideLock", "description": "Expose lock controls and live UWB data from commissioned UltraWideLock Matter devices."}},
    "abort": {"single_instance_allowed": "UltraWideLock is already configured."}
  },
  "options": {
    "step": {
      "init": {
        "title": "Credentials",
        "description": "Every observed UWB credential is collected automatically. Enter a friendly name and optionally create a separate presence sensor for it. Presence options are shown as presence__ followed by the credential ID.",
        "data": {
          "stale_timeout_seconds": "Clear stale UWB data after (seconds)"
        }
      }
    }
  }
}
